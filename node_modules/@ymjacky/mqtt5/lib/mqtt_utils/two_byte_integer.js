import * as UtilsError from "./error.js";
export function numToTwoByteInteger(num) {
  if (num > 0xFFFF) {
    throw new UtilsError.NumIsOutOfRange();
  }
  return Uint8Array.from([
    num >> 8,
    num & 0xFF
  ]);
}
export function twoByteIntegerToNum(buffer, offset) {
  return (buffer[offset] << 8) + buffer[offset + 1];
}
//# sourceMappingURL=two_byte_integer.js.map