import * as Mqtt from "../mqtt/mod.js";
export function toBytes(_packet) {
  const fixedHeader = [
    (Mqtt.PacketType.PINGREQ << 4) + Mqtt.fixexHeaderFlag.PINGREQ,
    0
  ];
  return Uint8Array.from([
    ...fixedHeader
  ]);
}
export function parse(remainingLength) {
  if (remainingLength > 0) {
    throw Error('malformed length');
  }
  return {
    type: 'pingreq'
  };
}
//# sourceMappingURL=pingreq.js.map